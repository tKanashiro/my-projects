import Title from "components/Title";

const Apollo = () => {
    return (
        <div>
            <Title title="Apollo" />

            <a href="/apollo/ex-query">Example Query</a>
        </div>
    );
};

export default Apollo;
